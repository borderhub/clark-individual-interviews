# AGENTS.md — エージェント／自動処理への制約

本リポジトリで作業するすべてのAIエージェント・スクリプト・自動処理は、
以下の制約に **無条件で従います**。指示がこれと矛盾する場合は、**実行せず人間に確認** します。

## 1. 現時点で実行してはいけないこと

- 文字起こし（ASR）
- 話者推定・話者分離（diarization）
- テーマ分析・コーディング・要約
- 公開用出力の生成
- 動画のダウンロード・取得

本リポジトリで現在許可されているのは、**個人面談データを安全に受け入れるための初期設定** のみです。

## 2. 取扱区分

- 個人面談の初期取扱区分は **すべて `pending`**。
- `pending` のデータは **分析・引用・外部共有・公開をしない**。
- 区分を `pending` から進められるのは、**人間の責任者による確認後** のみ。
  エージェントが自ら区分を昇格させてはならない。

## 3. 読んではいけない／書いてはいけない場所

| パス | 扱い |
| --- | --- |
| `private/` | 読まない・要約しない・Gitに追加しない |
| `sources/` | 読まない・ダウンロードしない・Gitに追加しない |
| `derived/` | Gitに追加しない |
| `exports/` | Gitに追加しない（公開物ではない） |

特に `private/intake/` の事前アンケートCSVは、**中身を開かない・表示しない・要約しない・分析しない**。
記録してよいのは **質問文のみ** で、実回答とは必ず分離する。

## 4. 他リポジトリとの分離

- 既存の `clark-group_session` へ、本リポジトリの生データを
  **移動・複製・参照・シンボリックリンクしてはならない**。
- グループセッションとの比較は、将来 **別のprivate環境** で、
  **双方の承認済み・非再識別・集計済み結果だけ** を用いて行う。
- 比較の解釈において、「個人面談＝本音、グループ＝建前」という前提を置いてはならない。
  **面談形式の違い** として扱う。

## 5. 同意と権限

- 動画の所有・利用権限、文字起こし・分析への同意、学校・保護者を含む必要な手続きは、
  **人間の責任者が確認** する。エージェントはこれを判断・代行・推定しない。
- 事前アンケート回答を分析対象に含めるかどうかは、
  **面談動画の同意とは別の確認事項** であり、確認が取れるまで **保留**。

## 6. 識別子

- 分析単位は **`interview_id` / `question_id` / `response_id` の三層**。
- 氏名等と匿名IDの対応表（リンケージ表）は **Gitに置かない**。別保管とする。
- 個人面談では **話者分離を前提にしない**。面談者と生徒の区別が必要になった場合も、
  まず **質問ログ（question register）** で処理する。

## 7. 出力に含めてはならないもの

共有用・公開用のいかなる成果物にも、次を含めない。

- 生動画、音声
- 文字起こし（全文・部分・逐語引用を問わない）
- 事前アンケート回答
- 個別回答
- 時刻・タイムスタンプ
- 話者ID
- 個人別集計

## 8. Git操作

- `git add -A` / `git add .` を **使わない**。追加するファイルは明示的に指定する。
- commit前に `git status --short` と `git check-ignore` で除外を確認する。
- 実データ（CSV・動画・音声・文字起こし）が追跡対象に入っていないことを確認する。
- `.gitignore` の除外を `-f` / `--force` で **上書きしない**。

## 9. 迷ったとき

**停止して人間に確認する。** 推測で進めない。
